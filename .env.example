# Wai Property Feasibility Study — Environment
# Copy this file to .env and fill in your values

PORT=3001
NODE_ENV=development
CLIENT_URL=http://localhost:5173

# Local Supabase (run: supabase start)
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImV4cCI6MTk4MzgxMjk5Nn0.EGIM96RAZx35lJzdJsyH-qQwv8Hj04zWl196z2-SB38

# Production (replace with your Supabase project values):
# SUPABASE_URL=https://<project-ref>.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=<service-role-key>

# Client-side (Vite) — prefix with VITE_
VITE_API_URL=http://localhost:3001/api

# PDF export — internal URL Puppeteer uses to load the page (Docker only)
# In docker-compose set to the client service name: http://client
# Defaults to CLIENT_URL if not set
# PDF_BASE_URL=http://client
